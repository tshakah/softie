<h1>Edit article</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.article_path(@conn, :update, @article)) %>

<span><%= link "Back", to: Routes.article_path(@conn, :index), class: "button" %></span>
