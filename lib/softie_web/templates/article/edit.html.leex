<h1>Edit article</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.article_path(SoftieWeb.Endpoint, :update, @article)) %>

<span><%= link "Back", to: Routes.article_path(SoftieWeb.Endpoint, :index), class: "button" %></span>
